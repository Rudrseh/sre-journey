apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: ClusterIP
  selector:
    app: python-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
      