apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  #type: LoadBalancer
  selector:
    app: my-app 
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      name: http
      