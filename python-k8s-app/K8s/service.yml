apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  #type: LoadBalancer
  type: NodePort
  selector:
    app: my-app 
  ports:
    - protocol: TCP
      port: 80
      nodePort: 30000
      name: http
      